/*const Plane = require('./plane.js');/

const plane1 = new Plane(1);
const plane2 = new Plane(2);

console.log(plane1.getPlaneId()); // Output: 1
console.log(plane2.getPlaneId()); // Output: 2 */


//AssertEquals - compares if two values are equal and returns true if they are and false if not. 

export function assertEquals(actualValue, expectedValue) {
    return actualValue === expectedValue;
}



